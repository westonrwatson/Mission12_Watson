// Weston Watson, Team 3, Section

export interface Book {
    bookID: number;  
    title: string;
    author: string;
    publisher: string;
    isbn: string;
    category: string;    
    classification: string;
    numberOfPages: number;
    price: number;
}